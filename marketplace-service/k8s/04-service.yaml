apiVersion: v1
kind: Service
metadata:
  name: marketplace-service
  namespace: prois
spec:
  type: ClusterIP
  selector:
    app: marketplace-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080